services:
  postgres:
    image: postgres:16
    container_name: tokyo_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: TokyoGuntungOnline290705
      POSTGRES_DB: tokyo_db
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pgdata: